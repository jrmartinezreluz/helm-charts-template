apiVersion: v2
name: hotel
description: Hospitality booking example — Argo Rollouts frontend (blue/green), stable backend, Traefik ingress
type: application
version: 0.3.0
appVersion: "1.0.0"
dependencies:
  - name: arkhadia-common
    version: "0.1.1"
    repository: "file://../library/arkhadia-common"
