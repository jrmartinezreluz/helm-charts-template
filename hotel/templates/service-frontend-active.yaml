apiVersion: v1
kind: Service
metadata:
  name: {{ include "hotel.fullname" . }}-frontend-active
  labels:
    {{- include "hotel.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend-router
spec:
  type: ClusterIP
  selector:
    {{- include "hotel.frontendSelectorLabels" . | nindent 4 }}
    {{- if not (include "hotel.rolloutEnabled" .) }}
    hotel/slot: {{ include "hotel.activeSlot" . }}
    {{- end }}
  ports:
    - name: http
      port: {{ .Values.service.frontendPort }}
      targetPort: http
