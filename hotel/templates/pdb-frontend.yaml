{{- include "arkhadia-common.pdb.manifest" (dict "ctx" . "name" (printf "%s-frontend" (include "hotel.fullname" .)) "selectorLabels" (include "hotel.frontendSelectorLabels" .) "pdb" .Values.frontend.podDisruptionBudget) }}
