apiVersion: v1
kind: Service
metadata:
  name: {{ include "n8n-platform.fullname" . }}
  labels:
    {{- include "n8n-platform.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "n8n-platform.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: n8n
