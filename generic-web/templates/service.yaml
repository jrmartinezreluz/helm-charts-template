apiVersion: v1
kind: Service
metadata:
  name: {{ include "generic-web.fullname" . }}
  labels:
    {{- include "generic-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "generic-web.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
